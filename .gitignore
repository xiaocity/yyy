# 注意：git 只认整行开头的 #，行尾注释不是注释，会被当成文件名的一部分。
# 所以下面每条注释都单独占一行。

# ============ 密钥与凭据：任何情况下都不进仓库 ============
# APK 签名私钥。泄露后别人就能签出被 Android 当作「本应用正式更新」的包，
# 而且没法吊销 —— 换密钥等于换应用，老用户装不了更新。
*.keystore
*.jks
*.p12
*.pem

# 本地签名口令，build.ps1 会自动读取
android/keystore.local.ps1

# ============ 个人签署件 ============
# 签名/印章扫描件，公开后可被截取用于伪造其它文件的签署
软著/签章.*

# ============ 构建产物 ============
android/build/

# 每次构建都从 index.html 重新复制
android/assets/

*.apk
*.apk.idsig
*.aab
羊羊羊之草原牧歌-小游戏.zip
files.zip

# 与 index.html 逐字节相同的分发副本，同步脚本生成
羊羊羊之草原牧歌.html

# ============ 临时与缓存 ============
__pycache__/
*.pyc
_chk.js
_c.js
node_modules/
.DS_Store
Thumbs.db
