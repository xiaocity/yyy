# 注意：git 只认整行开头的 #，行尾注释不是注释，会被当成文件名的一部分。
# 所以下面每条注释都单独占一行。

# ============ 密钥与凭据：任何情况下都不进仓库 ============
# 签名私钥。启用 Play 应用签名后，本地这把是「上传密钥」：丢了能向 Google 申请重置，
# 但泄露仍需立刻去 Console 重置，任何情况下都不该进仓库。详见 README「签名密钥」。
*.keystore
*.jks
*.p12
*.pem

# 本地签名口令，build.ps1 / build.sh 会自动读取
android/keystore.local.ps1
android/keystore.local.sh

# ============ 第三方个人信息 ============
# 封测测试者的邮箱名单。仓库是公开的，25 个真人的 Gmail 地址进了 git 历史就删不干净，
# 既是对他们的隐私问题，也不该由这个仓库来保管。留在本地即可。
test/
*testers_list*.csv

# ============ 个人签署件 ============
# 签名/印章扫描件，公开后可被截取用于伪造其它文件的签署
软著/签章.*

# ============ 构建产物 ============
android/build/

# 每次构建都从 index.html 重新复制
android/assets/

*.apk
*.apk.idsig
*.aab
羊羊羊之草原牧歌-小游戏.zip
files.zip

# 与 index.html 逐字节相同的分发副本，同步脚本生成
羊羊羊之草原牧歌.html

# ============ 临时与缓存 ============
__pycache__/
*.pyc
_chk.js
_c.js
node_modules/
.DS_Store
Thumbs.db
